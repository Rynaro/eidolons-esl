# PLACEHOLDER — SPECTRA owns this artifact's schema — see SPECTRA
# schemas/spec-profile.v1.json. This stub is a slot, not a schema.
# ESL only requires that a full-tier change carry spec.{md,yaml}; their shape
# is SPECTRA's, not ESL's. Replace this body with SPECTRA's spec.yaml output.
spectra_owned_slot: true
