<!--
  PLACEHOLDER — SPECTRA owns this artifact's schema.
  This is the ESL "lite" one-page slot, not a schema. The real one-page lite
  format is SPECTRA's; ESL only requires that a lite change carry a spec.md
  with GIVEN/WHEN/THEN + an acceptance-checks list. Replace this body with
  SPECTRA's one-page output.
-->
# <change_id> — lite spec

## Behavior

GIVEN: <precondition>
WHEN:  <trigger>
THEN:  <observable outcome>

## Acceptance checks

- AC-1: <criterion> — verify via <test-or-skill-ref>
